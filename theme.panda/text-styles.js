import { defineTextStyles } from '@pandacss/dev';

export const textStyles = defineTextStyles({
    'list-header': {
        description: 'Styles the title before lists',
        value: {
            fontWeight: 500,
            fontSize: 'lg'
        }
    }
})